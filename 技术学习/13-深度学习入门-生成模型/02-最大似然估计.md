

目标：如何使用正态分布拟合自然界的分布。
方法：最大似然估计（Maximum Likehood Estimation）

ps：第一个生成模型

## 生成模型的基础知识
**核心问题**
- 什么是生成模型
- 生成模型的目标是什么
- 如何创建生成模型

### 什么是生成模型
#生成模型
生成模型目标：对给定数据x的概率分布$p(x)$进行建模，然后生成新的模拟数据。
### 总体和样本
- 总体：对象的整体
	- 统计学的目标之一就是利用数量有限的样本对总体进行“估计”
- 生成模型：也是利用样本对总体进行估计，估计的对象是总体的概率分布，。

从样本估计总体概率分布的步骤：
1. 建模：假定真正的概率分布近似于参数可调整的概率分布。
2. 参数估计：估计参数，使概率分布拟合样本数据。

## 使用真实数据实现生成模型

略

## 最大似然估计的理论知识
#最大似然估计

估计正态分布参数的公式：
$$
\hat{\mu} = \frac{1}{N}\sum_{n=1}^N x^{(n)}
$$
$$
\hat{\sigma} = \sqrt{
\frac{1}{N}\sum_{n=1}^N(x^{(n)}-\hat{\mu})^2 
}
$$
- 有帽子的表示估计值。

### 似然的最大化

